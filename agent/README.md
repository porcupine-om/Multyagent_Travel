# AI-Агент Помощник Путешественника

AI-агент на Python 3.10.1, работающий через proxyapi с моделью OpenAI gpt-5-2025-08-07.

## Возможности

- 🌤️ **Получение погоды** для любого города
- 🔍 **Поиск информации** в интернете (достопримечательности, отели, транспорт, отзывы)
- ✅ **Составление чек-листов** для поездок на основе локации, дат и погоды (с автоматической отправкой файла)
- 💱 **Курсы валют** для разных стран
- 📱 **Генерация QR-кодов** для билетов и бронирований (с автоматической отправкой изображения)
- 📁 **Работа с файлами** (сохранение расписаний, чек-листов с автоматической отправкой)
- 🗓️ **Управление расписанием** и напоминаниями

## Установка

1. Убедитесь, что активировано виртуальное окружение с Python 3.10.1:
   ```bash
   .\venv\Scripts\Activate.ps1
   ```

2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Настройте переменные окружения в файле `.env` в корне проекта:
   ```
   PROXYAPI_API_KEY=your_api_key_here
   PROXYAPI_BASE_URL=https://api.proxyapi.ru/openai/v1
   EXCHANGERATE_API_KEY=your_exchangerate_key_here  # опционально
   BOT_TOKEN=your_telegram_bot_token  # для Telegram бота
   ```

## Запуск

### CLI интерфейс (терминал):
```bash
python run.py
```

### Telegram бот:
```bash
python bot.py
```

Для получения токена бота:
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в файл `.env`

## Примеры использования

После запуска вы можете задавать вопросы на естественном языке:

- "Какая погода в Москве?"
- "Сколько стоит доллар?"
- "Найди информацию о достопримечательностях Парижа"
- "Составь чек-лист для поездки в Токио с 1 по 10 января" *(бот автоматически отправит файл чек-листа)*
- "Сгенерируй QR-код для билета: ABC123" *(бот автоматически отправит изображение QR-кода)*
- "Найди отзывы об отелях в Барселоне"

## Структура проекта

```
agent/
├── agent.py          # Логика агента
├── tools.py          # Инструменты (погода, валюты, поиск, файлы, QR-коды)
├── memory.json       # Долговременная память агента
├── run.py            # CLI-интерфейс запуска
├── bot.py            # Telegram бот интерфейс
├── requirements.txt  # Зависимости
├── README.md         # Документация
└── INSTALL.md        # Инструкция по установке
```

## Инструменты

Агент использует следующие инструменты:

1. **WeatherTool** - получение погоды через open-meteo.com
2. **CurrencyTool** - курсы валют через exchangerate-api.com
3. **WebSearchTool** - поиск через DuckDuckGo
4. **FileIOTool** - работа с файлами
5. **TerminalTool** - безопасное выполнение команд
6. **QRCodeTool** - генерация QR-кодов
7. **MemoryTool** - сохранение контекста диалога

## Логирование

Все действия агента логируются. Логи выводятся в консоль с уровнем INFO.

## Telegram бот

### Команды бота:
- `/start` или `/help` - приветствие и справка
- `/status` - проверка статуса бота и агента

Все остальные сообщения обрабатываются как запросы к агенту.

### Автоматическая отправка файлов:

Бот автоматически отправляет пользователю все файлы, созданные агентом:

- **Чек-листы и текстовые файлы** - отправляются как документы для скачивания
- **QR-коды** - отправляются как изображения для просмотра и скачивания
- **Другие файлы** - отправляются в зависимости от типа (текст, изображение, документ)

Файлы сохраняются локально в папке `agent/` и автоматически отправляются пользователю после создания.

## Примечания

- Агент сохраняет историю диалога в `memory.json`
- Для работы с курсами валют можно использовать бесплатный API (без ключа)
- QR-коды сохраняются в файлы (PNG) и автоматически отправляются пользователю в Telegram
- Все созданные файлы (чек-листы, QR-коды) автоматически отправляются пользователю после создания
- Файлы сохраняются в папке `agent/` с абсолютными путями
- Терминальные команды ограничены для безопасности
- Telegram бот автоматически разбивает длинные ответы на части (лимит Telegram - 4096 символов)
- Бот поддерживает отправку различных типов файлов: текстовые документы, изображения, PDF и другие форматы

